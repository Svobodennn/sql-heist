// Single source of truth for site-wide SEO/metadata values. Consumed by the root
// layout (metadataBase + OG/Twitter), per-page metadata, sitemap, robots, and the
// JSON-LD structured data — so the canonical origin is defined in exactly one place.
export const SITE_URL = 'https://sqlheist.com'
export const SITE_NAME = 'SQL Heist'
export const SITE_TAGLINE = 'Learn SQL Injection by Pulling It Off'
export const SITE_DESCRIPTION =
  'A noir heist game that teaches SQL injection against a real in-browser SQLite engine — then teaches you to defend it. Three cases, eight objectives, zero setup, with optional progress sync.'
